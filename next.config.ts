import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable standalone output for Docker deployments
  // Uncomment the line below if you plan to use Docker
  // output: 'standalone',
  /* config options here */
};

export default nextConfig;
